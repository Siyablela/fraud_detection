apiVersion: v1
kind: Secret
metadata:
  name: fraud-redis
  labels:
    app.kubernetes.io/part-of: fraud-detection
type: Opaque
stringData:
  # Replace this value before applying to a real cluster.
  REDIS_PASSWORD: REPLACE_WITH_STRONG_PASSWORD
  REDIS_URL: redis://:REPLACE_WITH_STRONG_PASSWORD@redis:6379
